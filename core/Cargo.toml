[package]
name = "slm-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "WebAssembly numerical core for the SLM sequence compiler"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
